#ifndef _ATOMOS_LIBGEN_H
#define _ATOMOS_LIBGEN_H

/* Protótipos mínimos para satisfazer headers de bibliotecas externas. */

char *basename(char *path);
char *dirname(char *path);

#endif /* _ATOMOS_LIBGEN_H */

