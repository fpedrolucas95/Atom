[package]
name = "libimage"
license = "Apache-2.0"
publish = false
version = "0.1.0"
edition = "2021"
description = "Image decoding library for Atom OS (PNG, JPG, GIF) — no_std"
 
[lib]
name = "libimage"
path = "src/lib.rs"
 
[dependencies]
 
[dev-dependencies]