[package]
name = "atom_ui"
version = "1.0.0"
edition = "2021"
authors = ["Atom OS Contributors"]
description = "Atom UI — production-ready widget toolkit for the Atom Design System v1.0"

[dependencies]
atom_syscall = { path = "../syscall" }
atom_theme   = { path = "../atom_theme" }
libgui       = { path = "../libgui" }

[lib]
crate-type = ["rlib"]
