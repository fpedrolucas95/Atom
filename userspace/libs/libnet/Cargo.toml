[package]
name = "libnet"
license = "Apache-2.0"
publish = false
version = "0.1.0"
edition = "2021"

[dependencies]
atom_syscall = { path = "../syscall" }
libipc = { path = "../libipc" }

[lib]
crate-type = ["rlib"]
