[package]
name = "atom_abi"
license = "Apache-2.0"
publish = false
version = "0.1.0"
edition = "2021"
description = "Shared ABI constants between Atom kernel and userspace"
authors = ["Atom OS Contributors"]

[lib]
name = "atom_abi"
path = "src/lib.rs"

# This crate is no_std and has zero dependencies so it can be
# used by both the kernel and any userspace program.
